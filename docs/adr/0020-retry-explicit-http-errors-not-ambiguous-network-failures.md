# 只自動重試明確的暫時性 HTTP 回應

Provider 明確回覆 408、425、429 或 5xx 時，系統能判定該次呼叫未成功，可依既有退避策略自動重試並將每次 transport 嘗試寫入執行軌跡；fetch 網路例外則可能發生在 provider 已收到甚至完成計費、只有回應遺失的情況。為避免不確定狀態下重複付費，Multi-Agent 遇到網路例外時不得沿用現有自動重試行為，而應把生成執行標記為執行中斷，等待使用者明確重試該步驟。
