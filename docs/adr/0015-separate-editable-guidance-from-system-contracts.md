# 可編輯角色指令與固定系統契約分離

Planner 與 Critic 的輸出會驅動持久化、評分與路由，若使用者可刪除必要欄位或 JSON schema，生成執行將無法安全恢復；完全禁止自訂又不符合既有 Prompt 可調整的產品方向。因此偏好設定只開放各角色的角色指令，App 在執行時附加不可編輯、可版本化的系統契約。Writer 與 Editor 的固定契約要求純正文，Planner 與 Critic 的固定契約則定義必要輸入、JSON schema、草稿版本關聯與禁止事項。
