# 未結束生成執行引用的 LLM 設定檔不可刪除

生成上下文不保存 API Key，若刪除未結束流程所引用的 LLM 連線設定檔，恢復時將失去明確的 provider 與憑證來源；自動改用其他設定檔又會破壞同一執行的一致性。因此只要任何未結束生成執行仍引用設定檔，UI 就禁止刪除；設定不完整時流程進入設定阻塞，修復同一設定檔後才可繼續。所有引用流程均已採用或取消後即可刪除設定檔，歷史執行摘要與軌跡保留名稱、provider、model 等非敏感快照，不依賴已刪除設定檔。
