# 每次生成執行凍結生成上下文

持久化生成執行可能跨越數天，期間世界觀、角色、Wiki、參考章節、Prompt、rubric、門檻與模型設定都可能改變；若每一步讀取最新資料，同一執行的 Writer、Critic 與 Editor 將使用互不一致的前提，且無法重現路由。因此建立生成執行時保存不可變的生成上下文快照，後續步驟與恢復都沿用它；API Key 不進入快照，只記錄設定檔與非敏感模型資訊。來源變更時 UI 顯示上下文已過期，使用者只能繼續原流程或取消後以最新資料重新開始，不在原執行內刷新。
