# 未結束流程必須先取消才能刪除紀錄

直接刪除未結束生成執行會把停止外部請求與銷毀可觀測性證據合成一次不可逆動作，且可能讓稍後抵達的回應找不到歸屬。因此 UI 只允許刪除已結束的生成執行；使用者必須先明確取消未結束流程，再以獨立確認動作刪除。刪除會級聯移除該執行的摘要、步驟、檢查點、Prompt、回應與候選草稿，但不影響已採用的 `Chapter.content` 或正式 `ChapterVersion`。
