# 取消生成立即終止且保留軌跡

使用者取消生成時若仍等待目前 Agent 步驟完成，無法滿足停止成本與資源消耗的意圖；把取消當成暫停又會混淆可恢復狀態。因此取消會立即發出 `AbortSignal`、將生成執行標記為已取消並忽略任何稍後抵達的回應，正式正文保持不變，執行軌跡與摘要繼續保留且 UI 提示 provider 仍可能已計費。使用者先前明確採用的 Planner `beat`／`points` 不回滾，因為那是獨立授權的章節規劃變更。
