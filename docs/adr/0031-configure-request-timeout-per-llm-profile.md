# 每個 LLM 連線設定檔各自設定請求逾時

雲端模型與本機大模型的合理等待時間差異很大，沒有逾時又可能永久占用全 App 唯一的 LLM 執行槽。因此每個 LLM 連線設定檔提供 `timeoutSeconds`，預設 600 秒且限制在 30–3600 秒；executor 以 `AbortSignal` 執行逾時控制。逾時時無法確認 provider 是否已完成或計費，生成執行必須進入執行中斷且不得自動重試，等待使用者明確重試該步驟。
