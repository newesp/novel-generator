# FTS5 全文檢索 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Phase 2 收尾 — SQLite FTS5 桌面版全書搜尋 + Toolbar 🔎 UI + Wiki ingest Create 階段以 FTS 補相關章節段落。

**Architecture:** Tauri-only。Migration 003 建 `chapters_fts` / `wiki_pages_fts` virtual table + 6 個 trigger 自動同步；應用層走 `storage.search?` optional sub-store；Web (Dexie) 不掛 search，UI 自動隱藏。Tokenizer 用 SQLite 內建 `trigram`（中文友善、零外部依賴）。

**Spec:** `docs/superpowers/specs/2026-05-26-fts5-search-design.md`

**Tech Stack:** SQLite FTS5 (trigram) + tauri-plugin-sql + React 19 + 既有 vitest

---

## File Structure

| 檔 | 責任 |
|----|------|
| `src-tauri/migrations/003_fts.sql` | FTS5 virtual tables + 6 個 trigger + 一次性回填 |
| `src-tauri/src/lib.rs` | 註冊 migration 003 |
| `src/lib/search/types.ts` | `SearchHit` / `SearchOptions` / `SearchStore` 介面 |
| `src/lib/search/sanitize.ts` | 使用者 query → FTS5 安全字串 (+ test) |
| `src/lib/search/highlight.ts` | `<<<x>>>` → React `<mark>` (+ test) |
| `src/lib/search/fts-tauri.ts` | SQLite FTS5 實作 — chapters / wiki_pages 查詢 (+ test) |
| `src/lib/storage/types.ts` | StorageAdapter 加 `search?: SearchStore` |
| `src/lib/storage/tauri-sqlite-adapter.ts` | wire fts-tauri 進來 |
| `src/lib/storage/dexie-adapter.ts` | 不掛 search（capability flag） |
| `src/components/search/GlobalSearchModal.tsx` | 主 modal — input + scope tabs + result list |
| `src/components/search/SearchResultRow.tsx` | 單 row — icon + title + snippet + 點擊跳轉 |
| `src/components/Toolbar.tsx` | 加 🔎 按鈕 + 📚 Wiki 設定底部加 FTS 說明 |
| `src/lib/wiki-ingest.ts` | Create 分支前 storage.search?() 補章節段落 |
| `src/lib/prompt-defaults.ts` | wikiIngestCreateTemplate 加 `{{ftsExcerptsSection}}` 變數 |

---

## Task 1: Migration 003 — FTS5 schema + triggers + 回填

**Files:**
- Create: `src-tauri/migrations/003_fts.sql`
- Modify: `src-tauri/src/lib.rs`

### Why

整個 FTS 系統的基礎。Trigger 自動維護索引，所以後續 storage layer 寫入 chapters / wiki_pages 時不需特別處理。

- [ ] **Step 1: 寫 003_fts.sql**

Create `src-tauri/migrations/003_fts.sql`:
```sql
-- Phase 2 — FTS5 全文檢索（trigram tokenizer，CJK 友善）

-- ─────────────────────────────────────────────────
-- chapters FTS（chapters.data 是 JSON，走 json_extract）
-- ─────────────────────────────────────────────────
CREATE VIRTUAL TABLE chapters_fts USING fts5(
  chapter_id UNINDEXED,
  book_id UNINDEXED,
  title,
  content,
  tokenize='trigram'
);

CREATE TRIGGER chapters_ai AFTER INSERT ON chapters BEGIN
  INSERT INTO chapters_fts(chapter_id, book_id, title, content)
  VALUES (new.id, new.project_id,
          json_extract(new.data, '$.title'),
          json_extract(new.data, '$.content'));
END;

CREATE TRIGGER chapters_ad AFTER DELETE ON chapters BEGIN
  DELETE FROM chapters_fts WHERE chapter_id = old.id;
END;

CREATE TRIGGER chapters_au AFTER UPDATE OF data ON chapters BEGIN
  UPDATE chapters_fts
    SET title = json_extract(new.data, '$.title'),
        content = json_extract(new.data, '$.content')
    WHERE chapter_id = old.id;
END;

-- ─────────────────────────────────────────────────
-- wiki_pages FTS（有 native columns 直接用）
-- aliases 是 JSON array，replace 三層拆成空白分隔字串給 trigram 切
-- ─────────────────────────────────────────────────
CREATE VIRTUAL TABLE wiki_pages_fts USING fts5(
  page_id UNINDEXED,
  book_id UNINDEXED,
  title,
  aliases,
  description,
  content_md,
  tokenize='trigram'
);

CREATE TRIGGER wiki_pages_ai AFTER INSERT ON wiki_pages BEGIN
  INSERT INTO wiki_pages_fts(page_id, book_id, title, aliases, description, content_md)
  VALUES (new.id, new.book_id, new.title,
          replace(replace(replace(new.aliases, '[', ''), ']', ''), '"', ''),
          new.description, new.content_md);
END;

CREATE TRIGGER wiki_pages_ad AFTER DELETE ON wiki_pages BEGIN
  DELETE FROM wiki_pages_fts WHERE page_id = old.id;
END;

CREATE TRIGGER wiki_pages_au AFTER UPDATE ON wiki_pages BEGIN
  UPDATE wiki_pages_fts SET
    title = new.title,
    aliases = replace(replace(replace(new.aliases, '[', ''), ']', ''), '"', ''),
    description = new.description,
    content_md = new.content_md
    WHERE page_id = old.id;
END;

-- ─────────────────────────────────────────────────
-- 一次性回填現有資料
-- ─────────────────────────────────────────────────
INSERT INTO chapters_fts(chapter_id, book_id, title, content)
SELECT id, project_id,
       json_extract(data, '$.title'),
       json_extract(data, '$.content')
  FROM chapters;

INSERT INTO wiki_pages_fts(page_id, book_id, title, aliases, description, content_md)
SELECT id, book_id, title,
       replace(replace(replace(aliases, '[', ''), ']', ''), '"', ''),
       description, content_md
  FROM wiki_pages;
```

- [ ] **Step 2: 註冊 migration 003**

修改 `src-tauri/src/lib.rs`，在 migrations vec 末尾加：

```rust
Migration {
  version: 3,
  description: "FTS5 chapters/wiki_pages search index",
  sql: include_str!("../migrations/003_fts.sql"),
  kind: MigrationKind::Up,
},
```

完整 vec 後變成：
```rust
let migrations = vec![
  Migration {
    version: 1,
    description: "initial schema",
    sql: include_str!("../migrations/001_initial.sql"),
    kind: MigrationKind::Up,
  },
  Migration {
    version: 2,
    description: "wiki_pages and wiki_log",
    sql: include_str!("../migrations/002_wiki_tables.sql"),
    kind: MigrationKind::Up,
  },
  Migration {
    version: 3,
    description: "FTS5 chapters/wiki_pages search index",
    sql: include_str!("../migrations/003_fts.sql"),
    kind: MigrationKind::Up,
  },
];
```

- [ ] **Step 3: 跑一次 `npm run tauri dev` 確認 migration apply**

```bash
npm run tauri dev
```

預期：應用正常啟動、無 SQL error log。打開既有書（若有資料）應該不會壞掉。
若使用者沒有現有資料，純 cold start 也應該過。

關掉應用後到 `%APPDATA%\com.novelgenerator.app\novel-generator.db` 用 SQLite 工具（或 dev console `await window.dbDebug?.db` 取代）驗證：
- `SELECT count(*) FROM chapters_fts;` 應等於 `SELECT count(*) FROM chapters;`
- `SELECT count(*) FROM wiki_pages_fts;` 應等於 `SELECT count(*) FROM wiki_pages;`

若無 dev console 路徑，肉眼確認應用可用即可，trigger 正確性會在 Task 9 手動驗收測。

- [ ] **Step 4: Commit**

```bash
git add src-tauri/migrations/003_fts.sql src-tauri/src/lib.rs
git commit -m "feat(fts5): migration 003 — chapters/wiki_pages 全文索引

- chapters_fts virtual table (trigram tokenizer)，trigger 走
  json_extract(data) 因 chapters 主表用 JSON data 欄位
- wiki_pages_fts virtual table，trigger 直接用 native columns；
  aliases JSON 三層 replace 拆成空白分隔給 trigram
- 一次性回填現有 chapters / wiki_pages 資料
- src-tauri/src/lib.rs 註冊 migration version=3"
```

---

## Task 2: search types + sanitize.ts + test

**Files:**
- Create: `src/lib/search/types.ts`
- Create: `src/lib/search/sanitize.ts`
- Create: `src/lib/search/sanitize.test.ts`

### Why

`sanitize.ts` 是輸入消毒層，純函式、好測。types 是後續所有 search 模組共用契約。

- [ ] **Step 1: 寫 types.ts**

Create `src/lib/search/types.ts`:
```ts
export type SearchScope = 'chapter' | 'wikiPage' | 'both';

export interface SearchOptions {
  /** 預設 'both' */
  scope?: SearchScope;
  /** 預設 50 */
  limit?: number;
}

export interface SearchHit {
  scope: 'chapter' | 'wikiPage';
  id: string;
  /** 章節為章節 title；wiki 頁為 "type/slug — 顯示 title" */
  title: string;
  /** FTS snippet() 結果，含 <<<...>>> 標記 */
  snippet: string;
  /** bm25() 分數，越小越相關 */
  score: number;
  /** 章節 only：order_idx，方便顯示「第 N 章」 */
  chapterOrder?: number;
}

export interface SearchStore {
  search(bookId: string, query: string, opts?: SearchOptions): Promise<SearchHit[]>;
}
```

- [ ] **Step 2: 寫 sanitize 測試（先紅）**

Create `src/lib/search/sanitize.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { sanitizeFtsQuery } from './sanitize';

describe('sanitizeFtsQuery', () => {
  it('returns empty string for empty / whitespace input', () => {
    expect(sanitizeFtsQuery('')).toBe('');
    expect(sanitizeFtsQuery('   ')).toBe('');
  });

  it('quotes single word', () => {
    expect(sanitizeFtsQuery('老王')).toBe('"老王"');
  });

  it('quotes each space-separated word (AND semantics)', () => {
    expect(sanitizeFtsQuery('老王 劍術')).toBe('"老王" "劍術"');
  });

  it('preserves OR operator', () => {
    expect(sanitizeFtsQuery('老王 OR 趙六')).toBe('"老王" OR "趙六"');
  });

  it('strips FTS5-special characters that could break query', () => {
    expect(sanitizeFtsQuery('a"b*c(d)')).toBe('"abcd"');
  });

  it('handles mixed Chinese + OR', () => {
    expect(sanitizeFtsQuery('王大 OR 李四 OR 趙六')).toBe('"王大" OR "李四" OR "趙六"');
  });

  it('collapses multiple spaces', () => {
    expect(sanitizeFtsQuery('老王    劍術')).toBe('"老王" "劍術"');
  });

  it('ignores lowercase or as literal word', () => {
    // 'or' 小寫不是操作符，當普通字
    expect(sanitizeFtsQuery('老王 or 趙六')).toBe('"老王" "or" "趙六"');
  });
});
```

- [ ] **Step 3: 跑測試確認 fail**

```bash
npm test -- sanitize.test
```

預期：FAIL — `Cannot find module './sanitize'`

- [ ] **Step 4: 實作 sanitize.ts**

Create `src/lib/search/sanitize.ts`:
```ts
/**
 * 使用者輸入字串 → FTS5 安全的 query 字串
 *
 * 規則：
 * - 空字串 / 全空白 → 回空字串（上層短路）
 * - 用空白切詞，每個 token 過濾 FTS5 特殊字元（" * ( ) :）後 quote
 * - 大寫 OR 保留為操作符；其他 token 一律 quote
 * - 多個空白視為一個
 */
const FORBIDDEN_CHARS_RE = /["*():]/g;

export function sanitizeFtsQuery(input: string): string {
  if (!input || !input.trim()) return '';
  const tokens = input.trim().split(/\s+/);
  const parts: string[] = [];
  for (const raw of tokens) {
    if (raw === 'OR') {
      parts.push('OR');
      continue;
    }
    const clean = raw.replace(FORBIDDEN_CHARS_RE, '');
    if (!clean) continue;
    parts.push(`"${clean}"`);
  }
  return parts.join(' ');
}
```

- [ ] **Step 5: 跑測試確認通過**

```bash
npm test -- sanitize.test
```

預期：PASS（8 cases）

- [ ] **Step 6: Commit**

```bash
git add src/lib/search/types.ts src/lib/search/sanitize.ts src/lib/search/sanitize.test.ts
git commit -m "feat(search): types + sanitizeFtsQuery + 8 unit tests

- SearchScope / SearchOptions / SearchHit / SearchStore 介面
- sanitizeFtsQuery：空字串短路、單詞 quote、空白=AND、保留 OR 操作符、
  過濾 FTS5 特殊字元（\" * ( ) :）防注入"
```

---

## Task 3: highlight.ts + test

**Files:**
- Create: `src/lib/search/highlight.ts`
- Create: `src/lib/search/highlight.test.ts`

### Why

SQLite snippet() 回的 `<<<x>>>` 標記要在 React 渲染成 `<mark>` 元素。純字串 → ReactNode 轉換、好測。

- [ ] **Step 1: 寫 highlight 測試（先紅）**

Create `src/lib/search/highlight.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { renderToString } from 'react-dom/server';
import { highlightSnippet } from './highlight';

function renderText(node: React.ReactNode): string {
  return renderToString(<>{node}</>);
}

describe('highlightSnippet', () => {
  it('passes plain text through unchanged', () => {
    const out = renderText(highlightSnippet('沒有標記的純文字'));
    expect(out).toBe('沒有標記的純文字');
  });

  it('wraps single <<<x>>> as <mark>', () => {
    const out = renderText(highlightSnippet('前文<<<老王>>>後文'));
    expect(out).toBe('前文<mark>老王</mark>後文');
  });

  it('handles multiple marks', () => {
    const out = renderText(highlightSnippet('A<<<x>>>B<<<y>>>C'));
    expect(out).toBe('A<mark>x</mark>B<mark>y</mark>C');
  });

  it('escapes HTML special chars in surrounding text', () => {
    const out = renderText(highlightSnippet('<script>'));
    // renderToString 自動 escape
    expect(out).toBe('&lt;script&gt;');
  });

  it('handles ellipsis from SQLite snippet()', () => {
    const out = renderText(highlightSnippet('…前段<<<關鍵>>>後段…'));
    expect(out).toBe('…前段<mark>關鍵</mark>後段…');
  });

  it('empty string yields empty node', () => {
    const out = renderText(highlightSnippet(''));
    expect(out).toBe('');
  });
});
```

需要 `react-dom/server`。tsx 檔擴展：

把測試檔改名為 `highlight.test.tsx`（含 JSX）。

實際 path：`src/lib/search/highlight.test.tsx`

- [ ] **Step 2: 跑測試確認 fail**

```bash
npm test -- highlight
```

預期：FAIL — `Cannot find module './highlight'`

- [ ] **Step 3: 實作 highlight.ts**

Create `src/lib/search/highlight.ts`:
```ts
import type { ReactNode } from 'react';

const MARK_RE = /<<<(.+?)>>>/g;

/**
 * 把 SQLite snippet() 回傳的 `<<<x>>>` 標記轉成 React ReactNode 陣列，
 * 命中段落以 <mark> 包起來。
 *
 * @example
 * highlightSnippet('前<<<老王>>>後') → ['前', <mark>老王</mark>, '後']
 */
export function highlightSnippet(text: string): ReactNode {
  if (!text) return '';
  const parts: ReactNode[] = [];
  let lastIdx = 0;
  let match: RegExpExecArray | null;
  let key = 0;
  MARK_RE.lastIndex = 0;
  while ((match = MARK_RE.exec(text)) !== null) {
    if (match.index > lastIdx) {
      parts.push(text.slice(lastIdx, match.index));
    }
    parts.push(<mark key={key++}>{match[1]}</mark>);
    lastIdx = match.index + match[0].length;
  }
  if (lastIdx < text.length) parts.push(text.slice(lastIdx));
  return parts.length === 0 ? '' : parts;
}
```

副檔名要 `.tsx` 因為含 JSX。改名：`src/lib/search/highlight.tsx`

- [ ] **Step 4: 跑測試確認通過**

```bash
npm test -- highlight
```

預期：PASS（6 cases）

- [ ] **Step 5: Commit**

```bash
git add src/lib/search/highlight.tsx src/lib/search/highlight.test.tsx
git commit -m "feat(search): highlightSnippet — <<<x>>> → <mark> React node

純字串 → ReactNode 轉換，給 SearchResultRow 渲染 SQLite snippet()
結果用。6 個 unit test cover 無標記、單命中、多命中、ellipsis、
HTML 跳脫、空字串。"
```

---

## Task 4: fts-tauri.ts + test

**Files:**
- Create: `src/lib/search/fts-tauri.ts`
- Create: `src/lib/search/fts-tauri.test.ts`

### Why

實際呼叫 SQLite 的 search 實作。mock db 測試 row → SearchHit 形狀正確。

- [ ] **Step 1: 寫 fts-tauri 測試（先紅）**

Create `src/lib/search/fts-tauri.test.ts`:
```ts
import { describe, it, expect, vi } from 'vitest';
import { createFtsSearchStore } from './fts-tauri';

interface MockDb {
  select: ReturnType<typeof vi.fn>;
}

function makeMockDb(): MockDb {
  return { select: vi.fn() };
}

describe('createFtsSearchStore', () => {
  it('returns empty array for empty query', async () => {
    const db = makeMockDb();
    const store = createFtsSearchStore(async () => db as unknown as Parameters<typeof createFtsSearchStore>[0] extends (...args: never) => infer R ? R : never);
    const out = await store.search('b1', '', { scope: 'both' });
    expect(out).toEqual([]);
    expect(db.select).not.toHaveBeenCalled();
  });

  it('queries chapters_fts when scope=chapter', async () => {
    const db = makeMockDb();
    db.select.mockResolvedValueOnce([
      { id: 'ch1', chapter_order: 0, title: '王大入山', snippet: '…<<<老王>>>…', score: 1.2 },
    ]);
    const store = createFtsSearchStore(async () => db as never);
    const out = await store.search('b1', '老王', { scope: 'chapter' });
    expect(out).toEqual([
      {
        scope: 'chapter', id: 'ch1', title: '王大入山',
        snippet: '…<<<老王>>>…', score: 1.2, chapterOrder: 0,
      },
    ]);
    expect(db.select).toHaveBeenCalledTimes(1);
    const [sql, params] = db.select.mock.calls[0];
    expect(sql).toContain('chapters_fts');
    expect(sql).not.toContain('wiki_pages_fts');
    expect(params).toEqual(['"老王"', 'b1', 50]);
  });

  it('queries wiki_pages_fts when scope=wikiPage', async () => {
    const db = makeMockDb();
    db.select.mockResolvedValueOnce([
      { id: 'p1', type: 'entity', slug: 'wang-da', title: '王大', snippet: '…<<<老王>>>…', score: 0.8 },
    ]);
    const store = createFtsSearchStore(async () => db as never);
    const out = await store.search('b1', '老王', { scope: 'wikiPage' });
    expect(out).toEqual([
      {
        scope: 'wikiPage', id: 'p1', title: 'entity/wang-da — 王大',
        snippet: '…<<<老王>>>…', score: 0.8,
      },
    ]);
    const [sql] = db.select.mock.calls[0];
    expect(sql).toContain('wiki_pages_fts');
    expect(sql).not.toContain('chapters_fts');
  });

  it('queries both and sorts by score when scope=both', async () => {
    const db = makeMockDb();
    db.select
      .mockResolvedValueOnce([
        { id: 'ch1', chapter_order: 0, title: 'T1', snippet: 's1', score: 2.0 },
      ])
      .mockResolvedValueOnce([
        { id: 'p1', type: 'entity', slug: 's', title: 'PT', snippet: 's2', score: 1.0 },
      ]);
    const store = createFtsSearchStore(async () => db as never);
    const out = await store.search('b1', '老王');   // default scope both, limit 50
    expect(db.select).toHaveBeenCalledTimes(2);
    expect(out.map((h) => h.id)).toEqual(['p1', 'ch1']);   // 0.8 < 2.0 → wiki first
  });

  it('respects custom limit', async () => {
    const db = makeMockDb();
    db.select.mockResolvedValue([]);
    const store = createFtsSearchStore(async () => db as never);
    await store.search('b1', '老王', { scope: 'chapter', limit: 10 });
    const [, params] = db.select.mock.calls[0];
    expect(params[2]).toBe(10);
  });
});
```

- [ ] **Step 2: 跑測試確認 fail**

```bash
npm test -- fts-tauri.test
```

預期：FAIL — module not found

- [ ] **Step 3: 實作 fts-tauri.ts**

Create `src/lib/search/fts-tauri.ts`:
```ts
import type Database from '@tauri-apps/plugin-sql';
import { sanitizeFtsQuery } from './sanitize';
import type { SearchHit, SearchOptions, SearchStore } from './types';

interface ChapterFtsRow {
  id: string;
  chapter_order: number;
  title: string;
  snippet: string;
  score: number;
}

interface WikiFtsRow {
  id: string;
  type: string;
  slug: string;
  title: string;
  snippet: string;
  score: number;
}

/**
 * 工廠函式 — 給 TauriSqliteAdapter wire 用。
 * 拆成 factory 是為了 mock db 寫測試方便（不直接 import Database）。
 */
export function createFtsSearchStore(getDb: () => Promise<Database>): SearchStore {
  return {
    async search(bookId: string, query: string, opts?: SearchOptions): Promise<SearchHit[]> {
      const q = sanitizeFtsQuery(query);
      if (!q) return [];

      const scope = opts?.scope ?? 'both';
      const limit = opts?.limit ?? 50;
      const db = await getDb();
      const hits: SearchHit[] = [];

      if (scope === 'chapter' || scope === 'both') {
        const rows = await db.select<ChapterFtsRow[]>(`
          SELECT
            c.id,
            c.ord as chapter_order,
            chapters_fts.title as title,
            snippet(chapters_fts, 3, '<<<', '>>>', '…', 16) as snippet,
            bm25(chapters_fts) as score
          FROM chapters_fts
          JOIN chapters c ON c.id = chapters_fts.chapter_id
          WHERE chapters_fts MATCH ? AND chapters_fts.book_id = ?
          ORDER BY score LIMIT ?
        `, [q, bookId, limit]);
        for (const r of rows) {
          hits.push({
            scope: 'chapter', id: r.id, title: r.title,
            snippet: r.snippet, score: r.score,
            chapterOrder: r.chapter_order,
          });
        }
      }

      if (scope === 'wikiPage' || scope === 'both') {
        const rows = await db.select<WikiFtsRow[]>(`
          SELECT
            p.id, p.type, p.slug, p.title,
            snippet(wiki_pages_fts, 3, '<<<', '>>>', '…', 16) as snippet,
            bm25(wiki_pages_fts) as score
          FROM wiki_pages_fts
          JOIN wiki_pages p ON p.id = wiki_pages_fts.page_id
          WHERE wiki_pages_fts MATCH ? AND wiki_pages_fts.book_id = ?
          ORDER BY score LIMIT ?
        `, [q, bookId, limit]);
        for (const r of rows) {
          hits.push({
            scope: 'wikiPage', id: r.id,
            title: `${r.type}/${r.slug} — ${r.title}`,
            snippet: r.snippet, score: r.score,
          });
        }
      }

      // bm25 越小越相關，asc 排序
      hits.sort((a, b) => a.score - b.score);
      return hits.slice(0, limit);
    },
  };
}
```

- [ ] **Step 4: 跑測試確認通過**

```bash
npm test -- fts-tauri.test
```

預期：PASS（5 cases）

- [ ] **Step 5: Commit**

```bash
git add src/lib/search/fts-tauri.ts src/lib/search/fts-tauri.test.ts
git commit -m "feat(search): fts-tauri.ts — SQLite FTS5 search 實作

- createFtsSearchStore(getDb) factory 包 SearchStore
- chapters / wiki_pages 各自 SELECT 走 snippet() + bm25()
- scope='both' 時混合並依 bm25 score 排序
- 5 個 unit test mock db：空 query / scope=chapter / scope=wikiPage /
  scope=both 排序 / custom limit"
```

---

## Task 5: Storage adapter wire-up

**Files:**
- Modify: `src/lib/storage/types.ts`
- Modify: `src/lib/storage/tauri-sqlite-adapter.ts`

### Why

把 `createFtsSearchStore` 接進 TauriSqliteAdapter；Dexie adapter 不掛（自動 undefined）。上層用 `storage.search?` 判斷可用性。

- [ ] **Step 1: types.ts 加 search**

修改 `src/lib/storage/types.ts`：

頂部 import 加：
```ts
import type { SearchStore } from '../search/types';
```

`StorageAdapter` interface 末尾加：
```ts
export interface StorageAdapter {
  projects: ProjectStore;
  chapters: ChapterStore;
  versions: VersionStore;
  characters: CharacterStore;
  appMeta: AppMetaStore;
  wikiPages: WikiPagesStore;
  wikiLog: WikiLogStore;

  /**
   * Phase 2 FTS5 全文檢索（Tauri-only）。
   * 瀏覽器版 (Dexie) 為 undefined，上層用 `if (storage.search)` 守衛。
   */
  search?: SearchStore;

  replaceAll(bundle: StorageBundle): Promise<void>;
}
```

- [ ] **Step 2: TauriSqliteAdapter 掛 search**

修改 `src/lib/storage/tauri-sqlite-adapter.ts`：

頂部 import 加：
```ts
import { createFtsSearchStore } from '../search/fts-tauri';
```

找到 `export const tauriSqliteAdapter: StorageAdapter = { ... }`（或同等的對外 export），在 wikiLog 後面加 search field：

```ts
export const tauriSqliteAdapter: StorageAdapter = {
  // ... 既有
  wikiPages: { ... },
  wikiLog: { ... },
  search: createFtsSearchStore(getDb),
  replaceAll: async (bundle) => { ... },
};
```

注意 `getDb` 已存在 module 內，直接用。

- [ ] **Step 3: build 跑得過**

```bash
npm run build
```

預期：無 type error。Dexie adapter 不需改（不掛 search 即 undefined，符合 optional 型別）。

- [ ] **Step 4: Commit**

```bash
git add src/lib/storage/types.ts src/lib/storage/tauri-sqlite-adapter.ts
git commit -m "feat(search): wire FTS5 search 進 TauriSqliteAdapter

- StorageAdapter 加 optional search?: SearchStore
- TauriSqliteAdapter 用 createFtsSearchStore(getDb) 實作
- DexieAdapter 自動為 undefined（Web 版隱藏 UI）"
```

---

## Task 6: GlobalSearchModal + SearchResultRow

**Files:**
- Create: `src/components/search/SearchResultRow.tsx`
- Create: `src/components/search/GlobalSearchModal.tsx`

### Why

搜尋的 UI 層。Modal 含 input + scope tabs + result list；row 負責 icon + title + snippet + 跳轉。

- [ ] **Step 1: 寫 SearchResultRow.tsx**

Create `src/components/search/SearchResultRow.tsx`:
```tsx
import type { SearchHit } from '../../lib/search/types';
import { highlightSnippet } from '../../lib/search/highlight';

interface Props {
  hit: SearchHit;
  onClick: () => void;
}

const ICON: Record<SearchHit['scope'], string> = {
  chapter: '📖',
  wikiPage: '📚',
};

const SCOPE_BADGE: Record<SearchHit['scope'], string> = {
  chapter: '章節',
  wikiPage: 'Wiki',
};

export function SearchResultRow({ hit, onClick }: Props) {
  const chapterPrefix = hit.scope === 'chapter' && hit.chapterOrder != null
    ? `第 ${hit.chapterOrder + 1} 章 ` : '';
  return (
    <button
      type="button"
      onClick={onClick}
      style={{
        display: 'block', width: '100%',
        textAlign: 'left', padding: '10px 12px',
        background: 'transparent', border: 'none',
        borderBottom: '1px solid var(--border-subtle, #eee)',
        cursor: 'pointer', color: 'inherit',
      }}
    >
      <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'baseline', gap: 8 }}>
        <div style={{ fontSize: 13, fontWeight: 500 }}>
          <span style={{ marginRight: 6 }}>{ICON[hit.scope]}</span>
          {chapterPrefix}{hit.title}
        </div>
        <span style={{ fontSize: 10, color: 'var(--text-tertiary, #888)', flex: '0 0 auto' }}>
          {SCOPE_BADGE[hit.scope]}
        </span>
      </div>
      <div style={{
        fontSize: 12, color: 'var(--text-secondary)',
        marginTop: 4, lineHeight: 1.5,
      }}>
        {highlightSnippet(hit.snippet)}
      </div>
    </button>
  );
}
```

- [ ] **Step 2: 寫 GlobalSearchModal.tsx**

Create `src/components/search/GlobalSearchModal.tsx`:
```tsx
import { useState, useEffect, useMemo, useRef } from 'react';
import { Modal } from '../common/Modal';
import { storage } from '../../lib/storage';
import { useProjectStore } from '../../stores/projectStore';
import { useWikiStore } from '../../stores/wikiStore';
import { useUIStore } from '../../stores/uiStore';
import { SearchResultRow } from './SearchResultRow';
import type { SearchHit, SearchScope } from '../../lib/search/types';

interface Props {
  open: boolean;
  onClose: () => void;
}

export function GlobalSearchModal({ open, onClose }: Props) {
  const { project } = useProjectStore();
  const selectPage = useWikiStore((s) => s.selectPage);
  const setSelectedChapterId = useUIStore((s) => s.setSelectedChapterId);
  const setActiveTab = useUIStore((s) => s.setActiveTab);

  const [query, setQuery] = useState('');
  const [hits, setHits] = useState<SearchHit[]>([]);
  const [scope, setScope] = useState<SearchScope>('both');
  const [elapsedMs, setElapsedMs] = useState<number | null>(null);
  const [error, setError] = useState<string | null>(null);
  const inputRef = useRef<HTMLInputElement>(null);

  // open 時自動 focus input、重置狀態
  useEffect(() => {
    if (!open) return;
    setQuery('');
    setHits([]);
    setScope('both');
    setElapsedMs(null);
    setError(null);
    setTimeout(() => inputRef.current?.focus(), 50);
  }, [open]);

  // debounce 250ms
  useEffect(() => {
    if (!open || !project || !storage.search) return;
    if (!query.trim()) { setHits([]); setElapsedMs(null); setError(null); return; }
    const handle = setTimeout(async () => {
      try {
        const t0 = performance.now();
        const out = await storage.search!.search(project.id, query);
        setHits(out);
        setElapsedMs(Math.round(performance.now() - t0));
        setError(null);
      } catch (e) {
        setError((e as Error).message);
        setHits([]);
      }
    }, 250);
    return () => clearTimeout(handle);
  }, [query, open, project]);

  const filtered = useMemo(
    () => scope === 'both' ? hits : hits.filter((h) => h.scope === scope),
    [hits, scope],
  );

  const counts = useMemo(() => ({
    all: hits.length,
    chapter: hits.filter((h) => h.scope === 'chapter').length,
    wikiPage: hits.filter((h) => h.scope === 'wikiPage').length,
  }), [hits]);

  const jump = (h: SearchHit) => {
    if (h.scope === 'chapter') {
      setSelectedChapterId(h.id);
      setActiveTab('chapters');
    } else {
      selectPage(h.id);
      setActiveTab('wiki');
    }
    onClose();
  };

  return (
    <Modal open={open} onClose={onClose} title="🔎 全書搜尋" width={720}>
      <input
        ref={inputRef}
        type="text"
        className="form-input"
        placeholder="輸入關鍵字（多詞空白=AND、OR 大寫=或）"
        value={query}
        onChange={(e) => setQuery(e.target.value)}
        onKeyDown={(e) => { if (e.key === 'Escape') onClose(); }}
        style={{ width: '100%', padding: '8px 12px', fontSize: 14 }}
      />

      <div style={{
        display: 'flex', gap: 8, marginTop: 10, marginBottom: 8,
        fontSize: 12, alignItems: 'center',
      }}>
        {([
          { k: 'both' as const, label: `全部 ${counts.all}` },
          { k: 'chapter' as const, label: `章節 ${counts.chapter}` },
          { k: 'wikiPage' as const, label: `Wiki ${counts.wikiPage}` },
        ]).map(({ k, label }) => (
          <button
            key={k}
            type="button"
            onClick={() => setScope(k)}
            style={{
              padding: '4px 10px',
              background: scope === k ? 'var(--accent-bg)' : 'transparent',
              border: '1px solid var(--border)',
              borderRadius: 12, cursor: 'pointer',
              color: scope === k ? 'var(--text-primary)' : 'var(--text-secondary)',
            }}
          >{label}</button>
        ))}
        {elapsedMs != null && (
          <span style={{ marginLeft: 'auto', color: 'var(--text-tertiary)' }}>已搜尋 {elapsedMs} ms</span>
        )}
      </div>

      {error && (
        <div style={{
          padding: 8, background: 'rgba(255, 200, 50, 0.15)',
          fontSize: 12, marginBottom: 8,
        }}>查詢失敗：{error}</div>
      )}

      <div style={{ maxHeight: '60vh', overflowY: 'auto', border: '1px solid var(--border)' }}>
        {filtered.length === 0 && !error && (
          <div style={{ padding: 24, textAlign: 'center', color: 'var(--text-tertiary)' }}>
            {query.trim() ? '沒有命中' : '輸入關鍵字…'}
          </div>
        )}
        {filtered.map((h) => (
          <SearchResultRow key={`${h.scope}:${h.id}`} hit={h} onClick={() => jump(h)} />
        ))}
      </div>
    </Modal>
  );
}
```

- [ ] **Step 3: build 跑得過**

```bash
npm run build
```

預期：無 type error。

- [ ] **Step 4: Commit**

```bash
git add src/components/search/
git commit -m "feat(search): GlobalSearchModal + SearchResultRow

- Modal：input 自動 focus、debounce 250ms、scope tabs (全部/章節/Wiki)、
  已搜尋 N ms 顯示、ESC 關閉
- SearchResultRow：icon + 章節/Wiki badge + highlightSnippet 渲染
- jump 路由：章節 → setSelectedChapterId + setActiveTab('chapters')；
  wiki → selectPage + setActiveTab('wiki')
- error/empty state 提示"
```

---

## Task 7: Toolbar 🔎 按鈕 + 設定說明

**Files:**
- Modify: `src/components/Toolbar.tsx`

### Why

進入點接通；同時在 📚 Wiki 設定底部加 FTS 啟用說明。

- [ ] **Step 1: 看 Toolbar 既有結構**

```bash
grep -n "showBackupModal\|setShowBackupModal\|備份" src/components/Toolbar.tsx | head -5
```

確認 button group 與 modal mount 位置。

- [ ] **Step 2: 加 🔎 search 按鈕 + modal mount**

修改 `src/components/Toolbar.tsx`：

頂部 import 加：
```ts
import { GlobalSearchModal } from './search/GlobalSearchModal';
import { storage } from '../lib/storage';
```

component 內加 state（與 `showBackupModal` 同層）：
```ts
const [showSearchModal, setShowSearchModal] = useState(false);
```

button group（在 editor view 時顯示的 toolbar buttons 區域，💾 備份 按鈕附近）加：
```tsx
{view === 'editor' && project && storage.search && (
  <Button variant="secondary" onClick={() => setShowSearchModal(true)}>🔎 搜尋</Button>
)}
```

component return 末尾 mount（與 `<BackupModal>` 同層）：
```tsx
<GlobalSearchModal open={showSearchModal} onClose={() => setShowSearchModal(false)} />
```

- [ ] **Step 3: 在 📚 Wiki 設定底部加 FTS 說明**

找到 `activePrefsTab === 'wiki'` 那個 block 的末尾（lint 區段之後或之前都可，建議放最末尾分隔開）。

在 Lint 區段下方加：
```tsx
<hr style={{ margin: '12px 0', border: 'none', borderTop: '1px solid var(--border)' }} />
<div style={{
  fontSize: 12, color: 'var(--text-tertiary)', lineHeight: 1.6,
  padding: 8, background: 'var(--bg-tertiary)', borderRadius: 4,
}}>
  🔎 <strong>全書搜尋</strong>：本機桌面版自動啟用（SQLite FTS5 + trigram 分詞）；瀏覽器版不可用。<br/>
  Wiki ingest「存入」階段建立新頁時，會自動引用最多 3 章相關段落以提升首次建立品質。
</div>
```

- [ ] **Step 4: build 跑得過**

```bash
npm run build
```

- [ ] **Step 5: Commit**

```bash
git add src/components/Toolbar.tsx
git commit -m "feat(search): Toolbar 加 🔎 搜尋按鈕 + Wiki 設定加 FTS 說明

- editor view + 有 project + storage.search 存在時才顯示按鈕
  （Web 版自動隱藏）
- Modal 與 BackupModal 同層 mount
- 📚 Wiki 設定底部加灰色說明 box，告知 FTS 自動啟用範圍"
```

---

## Task 8: Wiki ingest Create 整合 FTS

**Files:**
- Modify: `src/lib/wiki-ingest.ts`
- Modify: `src/lib/prompt-defaults.ts`

### Why

情境 A — Create 階段以 FTS 補 ≤3 章相關段落，提升首次建立 wiki 頁品質。

- [ ] **Step 1: prompt-defaults.ts 加 ftsExcerptsSection 變數**

修改 `src/lib/prompt-defaults.ts`，找到 `DEFAULT_WIKI_INGEST_CREATE_TEMPLATE`，在 `## 本章節相關片段（資料來源）\n{{chapterExcerpt}}` 之後加：

```
{{ftsExcerptsSection}}

```

完整對應段落改後變成：
```ts
## 本章節相關片段（資料來源）
{{chapterExcerpt}}
{{ftsExcerptsSection}}

## 輸出格式（嚴格遵守）
```

注意 `{{ftsExcerptsSection}}` 變數為空字串時，這行就是一個空行，不影響 markdown 排版。

同檔末段 `PROMPT_TEMPLATE_VARS.wikiIngestCreateTemplate` 加說明：
```ts
{ var: 'ftsExcerptsSection', desc: 'FTS 抓到的全書相關段落（Tauri-only；無命中為空字串）' },
```

- [ ] **Step 2: wiki-ingest.ts 加 FTS 查詢**

修改 `src/lib/wiki-ingest.ts` 的 `applyOneOp` 函式 create 分支。

找到 `if (op.action === 'create') {` 區段，把：

```ts
const prompt = renderTemplate(aiPrompts.wikiIngestCreateTemplate, {
  type: op.type, slug: op.slug, title: op.title,
  aliasesList: op.aliases.join('、') || '(無)',
  reason: op.reason, contentBrief: op.content_brief,
  chapterExcerpt,
});
afterContent = await complete(prompt, { maxTokens: 2048 });
```

替換為：

```ts
// 情境 A：以 FTS 補 ≤3 章全書相關段落（Tauri-only；失敗不中止）
let ftsExcerptsSection = '';
if (storage.search) {
  try {
    const q = [op.title, op.slug.replace(/-/g, ' ')].join(' ');
    const hits = await storage.search.search(bookId, q, { scope: 'chapter', limit: 3 });
    const others = hits.filter((h) => h.id !== chapter.id);
    if (others.length > 0) {
      ftsExcerptsSection = '\n\n## 全書其他章節中提及的相關段落\n' +
        others.map((h) => `### 第 ${(h.chapterOrder ?? 0) + 1} 章「${h.title}」\n${h.snippet}`).join('\n\n');
    }
  } catch (e) {
    console.warn('[wiki-ingest] FTS lookup 失敗，跳過：', e);
  }
}

const prompt = renderTemplate(aiPrompts.wikiIngestCreateTemplate, {
  type: op.type, slug: op.slug, title: op.title,
  aliasesList: op.aliases.join('、') || '(無)',
  reason: op.reason, contentBrief: op.content_brief,
  chapterExcerpt,
  ftsExcerptsSection,
});
afterContent = await complete(prompt, { maxTokens: 2048 });
```

注意 update 分支**不改**（Update 既有頁不需要 FTS 補資料，避免 prompt 爆）。

- [ ] **Step 3: build 跑得過**

```bash
npm run build
```

- [ ] **Step 4: Commit**

```bash
git add src/lib/wiki-ingest.ts src/lib/prompt-defaults.ts
git commit -m "feat(search): Wiki ingest Create 階段補 FTS 段落（情境 A）

- applyOneOp 在 op.action='create' 分支前用 storage.search?.()
  抓全書相關章節段落（最多 3 章，過濾掉本章自己）
- 用 op.title + op.slug.replace(-, ' ') 當 FTS query
- 結果塞進 wikiIngestCreateTemplate 的 {{ftsExcerptsSection}} 變數
- FTS 失敗或 Web 版 storage.search 為 undefined 時，留空字串、不阻斷 ingest
- prompt-defaults.ts 加新變數 ftsExcerptsSection 進 template +
  PROMPT_TEMPLATE_VARS 說明

注意：使用者若已自訂過 #6 Wiki Create 模板，需到偏好設定 → AI 提示詞 →
#6 Wiki Create → ↺ 還原預設 才會生效。"
```

---

## Task 9: 手動驗收 sweep + CHANGELOG

**Files:** （無新增；驗收 + 文件）

### Why

對照 spec §4 手動驗收清單跑完一遍 + 補 CHANGELOG 條目。

- [ ] **Step 1: 跑全部 unit test 確認綠**

```bash
npm test
```

預期：3 個新測試檔（sanitize 8 + highlight 6 + fts-tauri 5 = 19）全綠；加上既有 lint 測試共 41 個 pass。

- [ ] **Step 2: build 確認無 warning / error**

```bash
npm run build
```

預期：build 成功。

- [ ] **Step 3: 跑 `npm run tauri dev` 開桌面版**

```bash
npm run tauri dev
```

確認 migration 003 apply 過、應用啟動正常。

- [ ] **Step 4: 跑 spec §4 手動驗收 11 條**

對照 `docs/superpowers/specs/2026-05-26-fts5-search-design.md` §4「手動驗收清單」逐一跑：

1. Tauri 桌面版 → Toolbar 看到 🔎；`npm run dev` 純瀏覽器版打開不見
2. 輸入「老王」→ 章節 + wiki 都列出，snippet 含黃底 mark
3. 輸入「老王 劍術」→ 兩詞都命中的段落才出現
4. 點章節結果 → 跳到該章；點 wiki 結果 → 跳到該 wiki 頁
5. 改章節 content 後立即重搜，新內容反映（trigger 生效）
6. 刪章節 → 重搜，該章不再出現
7. 改 wiki page contentMd → 重搜，新內容反映
8. 新建 wiki entity（透過章節 📚 存入 Wiki）→ 觀察 prompt log（若有 dev tool 取得 prompt）或檢查產出的 wiki page 內容是否有引用其他章節的資訊
9. 第一本書沒有相關段落 → ftsExcerptsSection 為空字串、prompt 不顯示空 header
10. Migration 003 在乾淨 DB 與既有 DB（含 chapters / wiki_pages）都跑得過、回填正確
11. （回歸）lint / wiki ingest / 章節生成現有功能不受影響

逐項打勾或記錄失敗。

- [ ] **Step 5: 補 CHANGELOG**

修改 `docs/CHANGELOG.md` 頂部加：

```markdown
## 2026-05-26 — Phase 2 收尾：FTS5 全書搜尋

完整實作 SQLite FTS5 桌面全文檢索（spec：`docs/superpowers/specs/2026-05-26-fts5-search-design.md`；plan：`docs/superpowers/plans/2026-05-26-fts5-search.md`）。

**新增功能**
- Toolbar 加「🔎 搜尋」按鈕（桌面版限定；Web 版自動隱藏）
- GlobalSearchModal：debounce 250ms input、全部/章節/Wiki scope tabs、SQLite snippet() 結果以 `<mark>` 渲染、點擊跳轉到 chapter / wiki 頁
- Wiki ingest Create 階段自動補 ≤3 章相關段落到 prompt（情境 A）

**Schema 變更**
- SQLite migration 003：`chapters_fts` / `wiki_pages_fts` virtual tables + 6 個 trigger 自動同步 + 一次性回填現有資料
- chapters trigger 走 `json_extract(data, '$.title' / '$.content')`，因 chapters 主表用 JSON `data` 欄位
- Tokenizer 用 SQLite 內建 `trigram`（中文友善、零外部依賴）

**架構**
- `src/lib/search/` 新模組：types / sanitize / highlight / fts-tauri，全純函式 + factory
- StorageAdapter 加 optional `search?: SearchStore`；Dexie 不掛 → Web 版自動 undefined
- 19 個新 unit test：sanitize (8) + highlight (6) + fts-tauri (5)

**已知限制**
- Web 版 (Dexie) 無 FTS5，UI 自動隱藏；要全文搜尋請用桌面版
- 不支援快捷鍵 Ctrl+K（後補）
- 不支援搜尋歷史 / regex / 大小寫切換
- Wiki ingest Update 分支不整合 FTS（避免 prompt 爆 token）
- 使用者若已自訂 #6 Wiki Create 模板，需手動「↺ 還原預設」才能用新變數 ftsExcerptsSection
```

- [ ] **Step 6: Commit + 完成**

```bash
git add docs/CHANGELOG.md
git commit -m "docs: CHANGELOG 補 Phase 2 收尾 FTS5 全書搜尋條目"
```

---

## Spec Coverage 自審

| Spec § | Plan task |
|--------|-----------|
| §1 Migration 003 schema | Task 1 |
| §1 lib.rs 註冊 | Task 1 step 2 |
| §1 trigger 走 json_extract for chapters | Task 1 step 1 |
| §1 trigram tokenizer | Task 1 step 1 |
| §2 SearchHit / SearchStore types | Task 2 |
| §2 sanitizeFtsQuery | Task 2 |
| §2 fts-tauri createFtsSearchStore | Task 4 |
| §2 storage adapter search wire-up | Task 5 |
| §2 Wiki ingest Create 整合（情境 A） | Task 8 |
| §2 prompt template 新變數 | Task 8 step 1 |
| §3 GlobalSearchModal | Task 6 |
| §3 SearchResultRow + 跳轉 | Task 6 |
| §3 Toolbar 按鈕 | Task 7 |
| §3 Wiki 設定加說明 | Task 7 step 3 |
| §3 錯誤處理（catch / web 版隱藏） | Task 6 + Task 8 |
| §4 unit tests（sanitize/highlight/fts-tauri） | Task 2 / 3 / 4 |
| §4 手動驗收 11 條 | Task 9 |
