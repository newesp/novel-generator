# 規格｜輸出格式

**Phase**：3（未實作）

---

| 格式 | 用途 |
|------|------|
| .txt | 純文字，方便進一步處理 |
| .html | 可在瀏覽器中閱讀，帶樣式 |
| .epub | 電子書格式，適配閱讀器；可選擇嵌入封面與插圖（Phase 4） |

> 目前專案尚未接入正式導出 pipeline。若後續實作，應從 `StorageAdapter` 讀取書本、章節與媒體 metadata；不要直接讀 Dexie 或 SQLite。
